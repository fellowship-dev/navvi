Local Apify storage: datasets, key-value stores, request queues, browser
profiles under `profiles/`. Profiles hold live sessions and cookies. This
folder is gitignored and must never be copied between machines.
